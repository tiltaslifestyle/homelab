---
- name: Prepare lab servers 
  hosts: lab_servers
  become: true
  
  roles:
    - role: common # Base utilities (git, curl, htop)
    - role: security # Hardening (SSH, UFW, Fail2Ban)

    # - role: incident-response # Tool for analyzing security incidents

    - role: docker # Container runtime environment
    - role: k3s # Lightweight Kubernetes