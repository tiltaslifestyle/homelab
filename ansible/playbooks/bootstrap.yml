---
- name: Prepare lab servers
  hosts: lab_servers
  become: true

  roles:
    - role: common            # Base utilities
    - role: security          # Hardening
    - role: incident_response # Tools
    - role: docker            # Container runtime
    - role: k3s               # Kubernetes
    - role: helm              # Package manager
    - role: agrocd            # GitOps
